name: Sync Whoop Data to Notion

on:
  schedule:
    # Runs daily at 6:30 AM UTC (30 minutes after GitHub sync)
    - cron: '30 6 * * *'
  workflow_dispatch:  # Allows manual triggering

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm install
        
      - name: Run Whoop sync script
        run: node whoop-sync.js
        env:
          WHOOP_ACCESS_TOKEN: ${{ secrets.WHOOP_ACCESS_TOKEN }}
          WHOOP_REFRESH_TOKEN: ${{ secrets.WHOOP_REFRESH_TOKEN }}
          WHOOP_CLIENT_ID: ${{ secrets.WHOOP_CLIENT_ID }}
          WHOOP_CLIENT_SECRET: ${{ secrets.WHOOP_CLIENT_SECRET }}
          WHOOP_NOTION_TOKEN: ${{ secrets.WHOOP_NOTION_TOKEN }}
          WHOOP_NOTION_DATABASE_ID: ${{ secrets.WHOOP_NOTION_DATABASE_ID }}  # ← Add this line